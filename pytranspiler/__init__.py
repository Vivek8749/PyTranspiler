"""
PyTranspiler — A basic Python to C++ transpiler.

Converts a subset of Python source code into compilable C++17 code
using Python's built-in `ast` module for parsing, a custom type-inference
analyzer, and a visitor-pattern-based C++ code generator.
"""

__version__ = "0.1.0"
